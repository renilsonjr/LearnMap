#3. Assign three variables in three separate lines, then print all three in one `print()` 
# call separated by commas.
date = "data de aniversario"
local_de_nascimento = "local de nascimento"
ano_de_nascimentp = "ano de nascimento"
print(date, local_de_nascimento, ano_de_nascimentp)
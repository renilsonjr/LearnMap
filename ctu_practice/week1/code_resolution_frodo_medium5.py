# 5. Write a script with an intentional syntax error inside a comment 
# (mismatched quotes in a `print()` call), then the corrected, 
# working version below it — same idea as the `basic_syntax` indentation-error exercise, 
# but for a quote mismatch instead.



#The scripts below has a mismatching " '
#print("school')
      
print('school')